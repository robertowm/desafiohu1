{{<layout.mustache}}
{{$body}}
<div class="container">
    <ul class="list-group">
        {{#result}}
            <li class="list-group-item">
                <span class="badge">{{availableRooms}}</span>
                {{name}}, {{city}}
            </li>
        {{/result}}
        {{^repo}}
            Não foi encontrado qualquer pacote relacionado.  Tente outro intervalo.
        {{/repo}}
    </ul>
</div>
{{/body}}
{{/layout.mustache}}